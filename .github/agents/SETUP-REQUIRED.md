<!-- AGENTTEAMS:BEGIN content v=1 -->
# SETUP-REQUIRED.md

The following **45 placeholder(s)** could not be automatically resolved
for project **LearnPythonStatsEcon** and require manual attention.

---

## 1. `{REFERENCE_DB_PATH}`

**Found in:** `multiple`
**Context:** The placeholder {REFERENCE_DB_PATH} could not be auto-resolved.

**Action required:** Search for `{MANUAL:REFERENCE_DB_PATH}` across all generated
agent files and replace with the correct value.

---

## 2. `{STYLE_REFERENCE_PATH}`

**Found in:** `multiple`
**Context:** The placeholder {STYLE_REFERENCE_PATH} could not be auto-resolved.

**Action required:** Search for `{MANUAL:STYLE_REFERENCE_PATH}` across all generated
agent files and replace with the correct value.

---

## 3. `{CONVERSION_PIPELINE}`

**Found in:** `multiple`
**Context:** The placeholder {CONVERSION_PIPELINE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:CONVERSION_PIPELINE}` across all generated
agent files and replace with the correct value.

---

## 4. `{PIP_PACKAGE_NAME}`

**Found in:** `multiple`
**Context:** The placeholder {PIP_PACKAGE_NAME} could not be auto-resolved.

**Action required:** Search for `{MANUAL:PIP_PACKAGE_NAME}` across all generated
agent files and replace with the correct value.

---

## 5. `{DOC_SITE_CONFIG_FILE}`

**Found in:** `multiple`
**Context:** The placeholder {DOC_SITE_CONFIG_FILE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:DOC_SITE_CONFIG_FILE}` across all generated
agent files and replace with the correct value.

---

## 6. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'SciPy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 7. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'SciPy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 8. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'SciPy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 9. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'Statsmodels' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 10. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'Statsmodels' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 11. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'Statsmodels' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 12. `{COMPONENT_SPEC}`

**Found in:** `ch1-essentials-expert.agent.md`
**Context:** Component 'Chapter 1 — The Essentials' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 13. `{COMPONENT_SECTIONS}`

**Found in:** `ch1-essentials-expert.agent.md`
**Context:** Component 'Chapter 1 — The Essentials' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 14. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch1-essentials-expert.agent.md`
**Context:** Component 'Chapter 1 — The Essentials' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 15. `{COMPONENT_SPEC}`

**Found in:** `ch2-lists-expert.agent.md`
**Context:** Component 'Chapter 2 — Working With Lists' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 16. `{COMPONENT_SECTIONS}`

**Found in:** `ch2-lists-expert.agent.md`
**Context:** Component 'Chapter 2 — Working With Lists' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 17. `{COMPONENT_SOURCES}`

**Found in:** `ch2-lists-expert.agent.md`
**Context:** Component 'Chapter 2 — Working With Lists' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 18. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch2-lists-expert.agent.md`
**Context:** Component 'Chapter 2 — Working With Lists' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 19. `{COMPONENT_SPEC}`

**Found in:** `ch3-numpy-pandas-expert.agent.md`
**Context:** Component 'Chapter 3 — NumPy, Pandas, and Matplotlib' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 20. `{COMPONENT_SECTIONS}`

**Found in:** `ch3-numpy-pandas-expert.agent.md`
**Context:** Component 'Chapter 3 — NumPy, Pandas, and Matplotlib' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 21. `{COMPONENT_SOURCES}`

**Found in:** `ch3-numpy-pandas-expert.agent.md`
**Context:** Component 'Chapter 3 — NumPy, Pandas, and Matplotlib' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 22. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch3-numpy-pandas-expert.agent.md`
**Context:** Component 'Chapter 3 — NumPy, Pandas, and Matplotlib' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 23. `{COMPONENT_SPEC}`

**Found in:** `ch4-functional-expert.agent.md`
**Context:** Component 'Chapter 4 — Functional Programming and Rudimentary Statistics' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 24. `{COMPONENT_SECTIONS}`

**Found in:** `ch4-functional-expert.agent.md`
**Context:** Component 'Chapter 4 — Functional Programming and Rudimentary Statistics' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 25. `{COMPONENT_SOURCES}`

**Found in:** `ch4-functional-expert.agent.md`
**Context:** Component 'Chapter 4 — Functional Programming and Rudimentary Statistics' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 26. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch4-functional-expert.agent.md`
**Context:** Component 'Chapter 4 — Functional Programming and Rudimentary Statistics' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 27. `{COMPONENT_SPEC}`

**Found in:** `ch5-probability-expert.agent.md`
**Context:** Component 'Chapter 5 — Probability Distributions' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 28. `{COMPONENT_SECTIONS}`

**Found in:** `ch5-probability-expert.agent.md`
**Context:** Component 'Chapter 5 — Probability Distributions' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 29. `{COMPONENT_SOURCES}`

**Found in:** `ch5-probability-expert.agent.md`
**Context:** Component 'Chapter 5 — Probability Distributions' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 30. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch5-probability-expert.agent.md`
**Context:** Component 'Chapter 5 — Probability Distributions' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 31. `{COMPONENT_SPEC}`

**Found in:** `ch6-hypothesis-expert.agent.md`
**Context:** Component 'Chapter 6 — Hypothesis Testing' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 32. `{COMPONENT_SECTIONS}`

**Found in:** `ch6-hypothesis-expert.agent.md`
**Context:** Component 'Chapter 6 — Hypothesis Testing' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 33. `{COMPONENT_SOURCES}`

**Found in:** `ch6-hypothesis-expert.agent.md`
**Context:** Component 'Chapter 6 — Hypothesis Testing' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 34. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch6-hypothesis-expert.agent.md`
**Context:** Component 'Chapter 6 — Hypothesis Testing' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 35. `{COMPONENT_SPEC}`

**Found in:** `ch7-ols-expert.agent.md`
**Context:** Component 'Chapter 7 — Building an OLS Regression Model' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 36. `{COMPONENT_SECTIONS}`

**Found in:** `ch7-ols-expert.agent.md`
**Context:** Component 'Chapter 7 — Building an OLS Regression Model' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 37. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch7-ols-expert.agent.md`
**Context:** Component 'Chapter 7 — Building an OLS Regression Model' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 38. `{COMPONENT_SPEC}`

**Found in:** `ch8-advanced-expert.agent.md`
**Context:** Component 'Chapter 8 — Advanced Data Analysis' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 39. `{COMPONENT_SECTIONS}`

**Found in:** `ch8-advanced-expert.agent.md`
**Context:** Component 'Chapter 8 — Advanced Data Analysis' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 40. `{COMPONENT_SOURCES}`

**Found in:** `ch8-advanced-expert.agent.md`
**Context:** Component 'Chapter 8 — Advanced Data Analysis' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 41. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch8-advanced-expert.agent.md`
**Context:** Component 'Chapter 8 — Advanced Data Analysis' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 42. `{COMPONENT_SPEC}`

**Found in:** `ch9-abm-expert.agent.md`
**Context:** Component 'Chapter 9 — Agent-Based Modeling' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 43. `{COMPONENT_SECTIONS}`

**Found in:** `ch9-abm-expert.agent.md`
**Context:** Component 'Chapter 9 — Agent-Based Modeling' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 44. `{COMPONENT_SOURCES}`

**Found in:** `ch9-abm-expert.agent.md`
**Context:** Component 'Chapter 9 — Agent-Based Modeling' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 45. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch9-abm-expert.agent.md`
**Context:** Component 'Chapter 9 — Agent-Based Modeling' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---


Once all items above are resolved, invoke `@conflict-auditor` to verify consistency.
<!-- AGENTTEAMS:END content -->
